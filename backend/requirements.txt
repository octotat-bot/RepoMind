# ── Web framework ───────────────────────────────────────────────────────────
fastapi==0.121.2
uvicorn[standard]==0.41.0
python-multipart==0.0.20

# ── Data validation / settings ──────────────────────────────────────────────
pydantic==2.12.4
pydantic-settings==2.12.0
email-validator==2.3.0

# ── Database ────────────────────────────────────────────────────────────────
SQLAlchemy==2.0.45
asyncpg==0.31.0
aiosqlite==0.21.0
greenlet==3.2.4

# ── Auth ────────────────────────────────────────────────────────────────────
PyJWT==2.12.1
bcrypt==5.0.0

# ── AI / RAG ────────────────────────────────────────────────────────────────
httpx==0.28.1
langchain-text-splitters==0.3.11
faiss-cpu==1.15.0
numpy==2.5.2

# In-process ONNX embeddings for hosts without a GPU or an Ollama daemon.
# Deliberately not sentence-transformers: this pulls no PyTorch, which is what
# keeps the image small enough for a free 512 MB instance.
fastembed==0.8.0

# ── Tests ───────────────────────────────────────────────────────────────────
pytest==9.0.1
pytest-asyncio==1.3.0
